/*
// Enable views for this room
view_enabled = true;

// Now set up the views

// First view -----------------------------------------------
// This includes the entire room
// Note that we only set visiblity and the viewport using the old view interface
// Everything else uses the new camera functionality
view_visible[0] = true;
view_xport[0] = 0;
view_yport[0] = 0;
view_wport[0] = 960;
view_hport[0] = 540;

// Create a camera for this view (this mirrors the old functionality)
view_camera[0] = camera_create_view(0, 0, view_wport[0], view_hport[0], 0, oPlayer, -1, -1, 1000, 1000);

*/